# Node.js dependencies
/node_modules

# Build output
# For Vite projects, the default build output is 'dist'
/dist
/dist-ssr # If you're using server-side rendering

# Environment variables
# These files should NEVER be committed as they often contain sensitive API keys or credentials.
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs/ # Catches a 'logs' directory if you have one

# Editor and OS specific files
.DS_Store # macOS
Thumbs.db # Windows
.idea/ # JetBrains IDEs (IntelliJ, WebStorm, etc.)
.vscode/ # VS Code settings directory
!.vscode/extensions.json # EXCEPT recommended extensions, which can be useful to share
*.suo # Visual Studio Solution User Options
*.ntvs* # Node.js Tools for Visual Studio
*.njsproj # Node.js Project files for Visual Studio
*.sln # Visual Studio Solution files
*.sw? # Vim swap files

# TypeScript build artifacts
# These are typically generated files that don't belong in source control
*.tsbuildinfo

# Vite specific
.vite/ # Vite cache directory

# Other common temporary files
*.bak
*.orig